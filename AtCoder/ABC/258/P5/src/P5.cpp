#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> pii;

int N, Q, X;
vector<ll> arr;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cin >> N >> Q >> X;
    arr.resize(N);
    for(int i = 0; i < N; i++){
        cin >> arr[i];
    }
    for(int i = 0; i < Q; i++){
        
    }
}